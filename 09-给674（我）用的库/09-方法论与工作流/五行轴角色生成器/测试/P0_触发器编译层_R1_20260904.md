---
type: five-axis-character-generator-p0-trigger-compiler-test
status: candidate
authority_level: L5
created: 2026-09-04
updated: 2026-09-05
may_override_canonical: false
parser_version: trigger-parser-v0.1
mapping_version: 世界观_机器映射_v0.1
---

# P0｜描述词触发器编译层 R1

## 本轮实现

基础触发器链：
- `数据/trigger_registry_v0.1.json`
- `实现/trigger_compiler_v0.1.js`
- `实现/trigger_compiler_v0.1.test.js`

后续 P0 数据门禁：
- `数据/mapping_registry_v0.1.json`
- `实现/data_compile_check_v0.1.js`
- `实现/data_compile_check_v0.1.test.js`
- `实现/repo_preflight_v0.1.js`
- `实现/repo_preflight_v0.1.test.js`

不修改任何 L1/L2 canonical、十元信息卡、准度卡、正式关系卡或 evidence 正本。

## 触发器成熟度

当前：**P0-beta / trigger runtime suite confirmed, data-preflight retest pending after fixture fix**。

已支持并归一化：
- `zn少女`
- `zn 少女`
- `ZN少女`
- `zn的少女`
- `xz少女`
- `x并z少女`
- `x+z少女`
- `x＋z少女`

规则：
- longest-match；`x并z` 永远作为单一 canonical symbol；
- 只有 `+ / ＋` 显式表示多 symbol；
- `zxzn少女` 这类相邻 canonical token 无连接符时返回 `ERROR_AMBIGUOUS_TOKEN`；
- unknown token 返回 `ERROR_UNKNOWN_TOKEN`；
- 空主体返回 `ERROR_EMPTY_SUBJECT`；
- 单 symbol 自动 `single`；多 symbol 自动 `multi`，但缺显式职责结构时返回 `BLOCKED_MULTI_RESPONSIBILITY_REQUIRED`；
- graph 只通过现有 runtime 的 nodes/relations/path 表达，不增加权重。

## resolved_request 最小字段

`raw_trigger / subject / canonical_symbols / mode / genre_context / seed / locked_modules / reroll_modules / parser_version / mapping_version`

## ACTUAL_RUNTIME｜2026-09-05 用户本地完整 trigger suite

用户在完整本地仓库环境重新执行：

```text
node "09-给674（我）用的库/09-方法论与工作流/五行轴角色生成器/实现/trigger_compiler_v0.1.test.js"
```

真实输出：

```json
{
  "tests": 120,
  "passed": 120,
  "failed": 0,
  "known_token_cases": 100,
  "parser_ambiguities_detected": 1,
  "x_parallel_z_split_errors": 0
}
```

- stderr：空
- exit code：`0`
- 结论：`ACTUAL_RUNTIME_TRIGGER_SUITE_PASS = true`
- known-token parse success：`100/100 = 100%`
- `x并z` 错拆：`0`
- unknown 静默吞掉：由 suite 负例门禁覆盖，未出现失败
- parser ambiguity：`1` 表示测试中的故意歧义输入被正确识别并拒绝，不表示遗留 parser bug
- same input/version/seed reproducibility：suite PASS
- locked module reroll integrity：suite PASS

因此旧状态 `FULL_REPO_TEST_RUNTIME_NOT_AVAILABLE` 对 trigger suite 已失效，不得继续引用为当前状态。

## ACTUAL_RUNTIME｜2026-09-05 数据门禁重测

### data_compile_check_v0.1.test.js

真实输出：

```json
{
  "tests": 36,
  "passed": 36,
  "failed": 0,
  "suite": "data_compile_check_v0.1",
  "actual_runtime": true
}
```

- stderr：空
- exit code：`0`
- 结论：`ACTUAL_RUNTIME_DATA_COMPILE_CHECK_PASS = true`

### repo_preflight_v0.1.test.js

真实输出：

```json
{
  "tests": 5,
  "passed": 4,
  "failed": 1
}
```

失败点：`valid fixture passes repo preflight`。

根因确认：测试 fixture 只有 `zx` 映射，但 `data_blocked_symbols` 漏登记无映射 canonical `x并z`。生产 checker 正确按 fail-closed 报 `DATA_PROJECTION_MISSING_BLOCKED_SYMBOL`；这是 fixture 缺陷，不是生产 registry/mapping 数据失败。

已修：`实现/repo_preflight_v0.1.test.js` 的合法 fixture 现将 `x并z` 纳入 `data_blocked_symbols`。修复后该 suite 需要本地再次真实运行，当前不得记为 PASS。

### repo_preflight_v0.1.js

用户旧 checkout 上真实运行：

```json
{
  "status": "PASS",
  "preflight_version": "repo-preflight-v0.1",
  "summary": {
    "registry": {"canonical_count": 10, "alias_count": 10},
    "mappings": {"mapping_count": 25, "rejected": 0, "pending": 5, "duplicate_structure_groups": []},
    "projection": {"mapping_version": "世界观_机器映射_v0.1", "registered_ids": 25},
    "coverage": {
      "usable_symbols": ["zx", "zn", "nz", "n"],
      "pending_only_symbols": ["z"],
      "data_blocked_symbols": ["x并z", "nx", "xn", "x", "xz"]
    },
    "runtime": null
  }
}
```

- stderr：空
- exit code：`0`
- 该结果证明当时本地数据包可通过真实 preflight。
- 但该输出仍是 25 mappings 且 `nx` blocked；current main 已新增 `nx × 世界观`，所以用户本地 checkout 明显落后于 current main。必须先拉取最新 main，再重跑 `repo_preflight_v0.1.test.js` 与 `repo_preflight_v0.1.js`，才能作为当前版本最终证据。

## 当前 P0 完成边界

已确认：

```text
trigger_compiler suite: 120/120 PASS
data_compile_check suite: 36/36 PASS
```

尚未确认 current main：

```text
repo_preflight_v0.1.test.js: fixture 已修，待本地重跑
repo_preflight_v0.1.js: 旧 checkout PASS，待 pull 最新 main 后重跑
```

因此当前状态仍不是 `P0 COMPLETE`。

## P0 端到端状态

当前：

`TRIGGER_RUNTIME_CONFIRMED / DATA_COMPILE_CONFIRMED / CURRENT_MAIN_PREFLIGHT_RETEST_PENDING`

不是：

`P0 COMPLETE`

## 下一断点

1. 用户本地 `git pull` 到 current main；
2. 重跑 `repo_preflight_v0.1.test.js`；
3. 重跑 `repo_preflight_v0.1.js` 对 current main 的真实 registry + mapping 全包预检；
4. 两项均绿后，汇总 P0 全部门禁并决定是否正式标记 `P0 PASS`。
