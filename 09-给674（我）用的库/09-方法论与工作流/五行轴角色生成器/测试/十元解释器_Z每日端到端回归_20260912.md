# 十元语义解释器 × Z 每日端到端回归｜2026-09-12

## 范围

- 写前 main: `86dff352f33df2749087c27343197d32127ec389`
- canonical / 十元信息卡 / 准度卡 / 正式关系卡 / behavior evidence: 只读
- 本轮仅新增应用层回归测试与本报告
- 目标：验证自然语言入口、Semantic IR、十元 decision、Z structural mutation、重新解释、nearest-neighbor guard 的闭环；不扩案例库

## 风险样本

沿用 `daily_e2e_regression_v0.1.test.js` 的 22 条版本化 fixture：

- 关键词诱导
- 职业 / 身份 / 题材 / 情绪诱导
- 同词不同 object_layer
- current_window 位移
- same changed_variable / different relation_source
- chosen path vs complete path-set
- blocked/recoverable vs exhausted
- state absence != opposite-pole presence
- 同轴与跨轴 nearest-neighbor
- x并z 原子化与显式 multi
- unknown / incomplete structure

## 发现的回归债

现有 22 条 E2E 仍使用安全 `extractSemanticIR()` 做表面词防误判，并用 caller-confirmed fields 进入 source / target IR。2026-09-11 新增的 `semantic_ir_ai_extraction_adapter_v0.1.js` 尚未进入每日端到端回归，因此“AI observation + evidence_span + confidence → IR”这条最新入口没有被每天覆盖。

本轮新增 `daily_e2e_ai_adapter_bridge_v0.1.test.js`，仅作为 runtime/test bridge，不写 canonical 语义规则，不把 mapping 当答案驱动器。

## ACTUAL_RUNTIME

环境：Node `v22.16.0`。

直接 `git clone` current-main 仍因执行环境 DNS 失败：

```text
fatal: unable to access 'https://github.com/lukunanaron674-stack/-ten-yuan-vault.git/': Could not resolve host: github.com
```

因此未宣称完整 current-main repository suite PASS。

随后通过 GitHub connector 逐个回读 `86dff352...` 的目标 runtime 源码/blob，在临时目录重建 focused runtime 并真实执行 Node。

### 现有 daily E2E stdout 摘要

```text
suite = daily_e2e_regression_v0.1
samples = 22
ir_field_guess_rate = 0
keyword_lure_false_positive_rate = 0
nearest_neighbor_confusion_rate = 0
object_layer_drift_rate = 0
window_drift_rate = 0
opposite_pole_inference_error_rate = 0
z_reskin_rejection_rate = 1
z_retrieval_only_rejection_rate = 1
z_structural_mutation_success_rate = 1
reinterpretation_stability = 1
unknown_silent_swallow = 0
x_parallel_z_split_errors = 0
source = nx
mutation = READY
revalidated = xn
counterexample = AMBIGUOUS
unknown neighbor = NEEDS_MORE_STRUCTURE
e2e = PASS_FOCUSED_CHAIN
```

### 新 AI adapter bridge stdout

```text
suite = daily_e2e_ai_adapter_bridge_v0.1
samples = 22
ir_field_guess_rate = 0
source_known_fields = 12
source_symbol = nx
z_reskin_rejected = REJECTED_RESKIN
z_retrieval_only_rejected = REJECTED_RETRIEVAL_ONLY
z_mutation = READY
target_known_fields = 12
revalidation.status = PASS
revalidation.symbol = xn
revalidation.gate_source = REGISTRY_VERIFIED
e2e = PASS_AI_EXTRACTION_BRIDGE
```

## 核心指标

| metric | result |
|---|---:|
| ir_field_guess_rate | 0 |
| keyword_lure_false_positive_rate | 0 |
| nearest_neighbor_confusion_rate | 0 |
| object_layer_drift_rate | 0 |
| window_drift_rate | 0 |
| opposite_pole_inference_error_rate | 0 |
| z_reskin_rejection_rate | 1.0 |
| z_retrieval_only_rejection_rate | 1.0 |
| z_structural_mutation_success_rate | 1.0 |
| reinterpretation_stability | 1.0 |
| unknown_silent_swallow | 0 |
| x_parallel_z_split_errors | 0 |

## 结论

- 22 条表面诱导输入仍不会仅凭职业、题材、情绪、名字或十元表面词填 IR。
- `x并z` longest-match / atomic guard 未回归。
- reskin 与 retrieval-only 均继续拒绝。
- `SHIFT_OBJECT_LAYER` 只改变 `object_layer`，其余 frozen fields 未漂移。
- 新 bridge 证明 evidence-backed AI adapter 可以先形成 12-field source IR，再完成 `nx -> object_layer mutation -> xn`，并在生成候选后再次走 AI adapter + production registry revalidation，而不是把生成文本直接当结论。
- nearest-neighbor counterexample 仍返回 AMBIGUOUS，UNKNOWN review 仍返回 NEEDS_MORE_STRUCTURE。

## 代表性失败 / 归属

本轮无新的解释器 semantic failure，也无新的 Z operator structural failure。

实际发现的是测试覆盖债：`semantic_ir_ai_extraction_adapter_v0.1` 已落库但未进入 daily E2E。归属：解释器测试线，而非 canonical 理论错误。

## 状态边界

`PASS_FOCUSED_CHAIN` 与 `PASS_AI_EXTRACTION_BRIDGE` 只表示 focused runtime 通过。

由于完整仓库 checkout / complete current-main runtime suite 未真实执行，P0 PASS 仍禁止，状态保持：

`AWAITING_FULL_CURRENT_MAIN_RUNTIME_CONFIRMATION`

## 下一日最高风险

AI adapter bridge 当前使用 deterministic stub provider 验证 evidence/provenance contract；最大风险已经转到真实 provider 的 paraphrase stability：同一结构换说法后，12-field IR 是否只发生合理变化，以及低置信度 / 缺证据是否持续退回 unknown，而不是为了“看起来聪明”乱补字段。
