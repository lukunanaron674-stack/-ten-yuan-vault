# B3｜本地 Codex 执行任务书（2026-09-20）

> 任务：利用已提交的 B1/B2/B3 文件完成封闭盲测执行环境的工程验收，优先用本地 Codex，不必先启动云端 4090。此文件是执行任务书，不代表本机已经完成操作。

## 项目与现有文件

仓库：`lukunanaron674-stack/-ten-yuan-vault`。复用以下已有文件，**不重新建立仓库、不重复创建理论定义，不覆盖现有十元/五轴正本**：

- `01-十元系统/05-十元语义空间/B1_R5关系输入审计_20260920.json`
- `tools/r5_compiler_core_v0_1.mjs`、`tools/r5_compiler_cli_v0_1.mjs`、`tests/r5_compiler_v0_1.test.mjs`
- `tools/r5_b3_predictor.mjs`、`tools/r5_b3_evaluator.mjs`、`tests/r5_b3_sealing_v0_1.test.mjs`
- `01-十元系统/05-十元语义空间/B3_Pilot20分层计划_20260920.json`
- `01-十元系统/05-十元语义空间/B3_独立封闭测试执行说明_20260920.md`
- `01-十元系统/05-十元语义空间/B3_验收状态_20260920.json`

## 先保护本地仓库

1. 先确认本地仓库工作区、未提交文件和本地/远端分支是否分叉。用户此前出现过体积较大的分叉历史。**不要直接执行破坏性 git reset --hard、git clean、git push --force，也不要覆盖本地 Canvas、PNG 或笔记。**
2. 如果不能安全直接 pull，使用 fetch + 查看差异、建立本地备份分支或安全工作树，再导入本任务书及 B3 相关变更。输出真实的本地同步结果，不要把“远端已提交”说成“本地已同步”。
3. 本地只需要 Node.js 与 Codex 即可启动工程检查。若现有千问 API 可用，不要求为出题专门启动4090。

## 第一阶段：本地工程验收

1. 执行 `node --test tests/r5_compiler_v0_1.test.mjs` 和 `node --test tests/r5_b3_sealing_v0_1.test.mjs`，记录实际通过/失败项及日志；修复运行时错误后重复测试。
2. 在预测者不可访问的**外部私有位置**准备 GOLD 存储；预测工作树仅放 INPUT、已锁定关系规则及 PRED。仅将 PRIVATE VAULT 放到另一个目录不算隔离，检查预测执行账号、容器、连接器和调用工具的**实际读取权限**。
3. 用显式标注的 MOCK DATA 验证事前封存 → INPUT 传递 → B2六字段预测 → PRED冻结 → 独立揭晓 → 两名评分者评分 → 输出报告。检验改写、重复提交、先看答案、评分者身份冲突等失败路径。
4. 对 B3 时间戳与哈希、权限假设和异常路径进行审计；程序自称“sealed”不能替代真实访问权限。检查文件哈希使用相同的标准化方式，并确认评测执行时未向预测进程泄漏参考后续。
5. 不许将模拟分数、公开案例开发集或模型自评当作真正盲测成绩。

## 第二阶段：20例 Pilot 分层出题

1. 读取已有 Pilot20 题槽，不重复生成相同题。优先使用已经连接且可用的千问 API 作为独立出题端；如果需要云端4090运行私有千问模型，再报告启动云端的实际理由。
2. 由出题端在预测开始之前创建原创 INPUT 与既定 GOLD；GOLD 仅写入独立评分者可读、预测者不可读的存储，记录出题版本、来源、时间和预先封存的哈希。不要把 GOLD 上传到本仓库、Obsidian 或 Linear 可被预测端读取的位置。
3. 预测端是独立模型或受限会话，只读取 INPUT 与冻结关系规则，输出六字段、现实变量、动作链、状态变化与最小单帧；预测落盘后不得修改。
4. 评分端在预测锁定后读取 GOLD 与 PRED；两名独立评分者分别评分，分歧另行复核。独立性须由实际访问权限和运行隔离保障，不能只靠两个聊天框或模型自报“未看答案”。
5. Pilot 用于校准出题及程序。调过规则的 Pilot 样本不转入正式100例 C-pool。正式 C-pool 需要独立创建的新样本，且不得由同一模型出题、作答、评分后自证独立。

## 第三阶段：验收及回报

交付本地测试记录、实现的权限隔离方案、真实20例 Pilot 各阶段计数、失败日志和阻塞项，说明现在是否有必要启动4090。未经实际核验不得宣布20例已完成。

当前已知状态：20/20题槽规划完成；独立 INPUT 0/20；真实 GOLD 封存 0/20；预测冻结及双评分 0/20；正式 SEALED BLIND 0/100。

## 本地 Codex 启动句

请从本任务书开始，在现有本地五轴仓库执行第一阶段工程验收；保护未提交的本地材料，禁止重设项目。完成第一阶段后按当前可用的独立模型及访问权限推进20例Pilot，并用真实日志报告结果。
