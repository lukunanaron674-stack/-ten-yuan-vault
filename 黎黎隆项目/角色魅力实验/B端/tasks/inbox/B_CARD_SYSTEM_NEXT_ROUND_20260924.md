---
task_id: B_CARD_SYSTEM_NEXT_ROUND_20260924
title: 下一轮接入统一 CH 卡系统
created_date: 2026-09-24
status: queued_for_next_round
requested_by: user
scope: B端下一个尚未开始的新实验；不追改进行中任务
---

# 下一轮任务：统一卡系统接入 + 单一十元 ZX 首卡

## 用户已确认的决策
- 废弃此前**拟定但未正式注册**的 `B-SINGLE-ZX-001` 命名方案。
- 使用 `CH-` + 六位流水号作为新的永久卡号；角色、十元、实验模式、场景、规则版本、执行轮次、阶段、生成抽样都放在独立字段，不编码进永久卡号。
- B1、B2、B3、4090、B4 共享同一 `card_id`，分别维护各自有版本的产物；不得各自分配永久卡号。
- 旧 R35/R37/R45 及原来 B1/B2/B3/B4 文档和结果全部保留，使用 `legacy_task_id` / `source_ref` 关联，不改名、不覆盖。
- 下一轮从单一十元 ZX 小实验起步，不强加第二十元或生克补关系；保持原角色和场景设计，10–11 秒、成片 16:9 横屏，角色参考图可为 9:16，run_A/run_B 同输入双抽。
- 所有规则与任务修改必须记录父版本、规则版本、修改前后内容、原因、时间、影响范围和实际 GitHub commit。禁止凭空宣称提交或完成。

## 下一轮执行顺序
1. 首先完整核对仓库现存卡片目录、索引及已有 `CH-` 编号；GitHub 代码搜索无结果不等于全库不存在。确认命名无冲突后才分配第一个空闲号，候选 `CH-000001` 不是已占用编号。
2. 建立单一权威卡索引 `CARD_INDEX.json`（放在 B 端卡片系统目录，并在 README 写明权威路径）；包含 schema_version、next_seq、已分配 card_id、状态、当前卡片版本、关联角色/场景/实验类型、原任务 ID、各阶段产物相对路径。对并发分配采用串行写入或 compare-and-swap / 冲突检测；不得多个 worker 分别推测 next_seq。
3. 建立卡模板及编号、版本、轮次规范：`card_id` 永不更改；`card_version` 仅代表卡片内容修订，`rule_version` 代表采用哪版规则，`round_id` 为该卡执行轮次，`run_A/run_B` 为该轮生成抽样；不得混用。登记每次修订的 diff 和原始输入引用。
4. 以实际分配到的首个空闲 `CH-xxxxxx` 创建“黎黎隆 / ZX / single_tenyuan”实验主卡；B1/B2/B3/4090/B4 所有新文件统一引用该卡号和实际版号。勿把 `CH-000001` 在未分配前写成已注册事实。
5. 读取《视频十元描述词转化_v1.0.md》与新版《视频十元描述词转化_v1.1.md》后，再进行 B2/B3 编译。新版 MD 若尚未进入仓库，必须先明确取得该文件的真实内容和可核实版本，禁止按本任务概述臆造其全文或声称它已入库。保留 v1.0，不覆盖。
6. B2 只预登记 ZX 的可观察动作与时序假设，B3 由该假设决定镜头/运动/光影/时间变化节点，按照 H3 workflow 的实际 fps 编译整数帧；不可固定按每两秒分镜，不可事后给动作乱贴十元。H3 执行正文不含十元内部术语。
7. 4090 仅在输入素材真实绑定、目标尺寸和实际 fps 已核实后运行；按十元变化节点回传指定帧与真实帧号/时间、输入 SHA 和执行参数。B4 区分设计、文本交接、生成执行、离散关键帧证据和完整视频时序证据；不得以已回传文件代替实际视觉审核。
8. 完成后在下一轮日志明确报告：卡索引路径、实际首卡号、规则文件路径、各产物版本及 GitHub commit、未执行项与阻塞项。任务排队不等于已经分配卡号、注册首卡或完成实验。

## 验收条件
- [ ] 唯一权威索引落库、编号查重和冲突机制可用
- [ ] 首卡成功注册，B1–B4 全部引用同一个 `card_id`
- [ ] v1.0 保留，v1.1 的实际内容可核实且有版本差异记录
- [ ] ZX 单元实验不混入生克补及额外十元
- [ ] 文件和提交 SHA 如实回填，执行/画面未验证则标明未验证
