# ADV_B_20260925_0307_R51｜WORK VALIDATION

## 先验绑定闸门

- 绑定 Picture 1 后回写真实路径、SHA-256、尺寸，并判断同一角色与H3排版。
- 绑定 Picture 2 后回写真实路径、SHA-256、尺寸与场景几何。
- 只有原图可见既有植被或水面时，才保留2–4秒风动刺激；否则停止任务，不新增物件。
- 确认角色胸前确有可读的既有背带；没有则停止任务，不生成新背带。

## 执行

- 使用同一完整prompt与同一输入分别执行 run_A、run_B。
- 支持seed则回写真实seed；不支持则分别记录first execution、second execution。
- 禁止引用旧R34/R31的seed、task_id或输出路径作为R51结果。

## 验收

1. 仅一个角色、一个物理身体、一个场景。
2. 16:9横屏，固定全身中远景，固定光线，无运镜、切镜或转场。
3. 0–2秒已稳定；2–4秒唯一外部刺激；4–6秒局部衣料反应；6–8秒眼先于头；8–10秒仅一次既有背带轻按；10–11秒完全停住。
4. 不新增背带、扣具、场景物件或天气。
5. 回写节点证据、音轨状态和盲审原话；无法观察则标INDETERMINATE，不推断。

当前：pending_image_validation，实际0/2。
