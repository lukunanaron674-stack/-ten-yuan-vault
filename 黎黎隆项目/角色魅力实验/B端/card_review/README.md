# B 端卡片首帧审核网页 v1.1

状态：CH-ZX-001 已正式注册，可进入 B1；真实渲染帧和视觉审核仍待 Work 输出。

## 权威文件

- `CARD_INDEX.json`：唯一权威卡号索引，路径为 `黎黎隆项目/角色魅力实验/B端/card_review/CARD_INDEX.json`
- `cards/CH-ZX-001.json`：首张新卡的登记文件
- `cards.json`：审核网页读取的卡片目录
- `index.html`：首中尾帧审核页面

## CH-ZX-001 固定规则

- 角色：黎黎隆
- 单一十元：ZX
- 不强加第二十元
- 不强制生克补作为主轴
- 最终成片：16:9 横屏
- 角色参考图：允许 9:16
- 时长：10–11 秒
- 每条描述词：run_A / run_B 两次
- B1、B2、B3、4090、B4、B5 共用 `CH-ZX-001`，并统一素材、版本、轮次与验收

参考图画幅不能替代最终输出画幅判断：角色图为 9:16 不代表成片必须竖屏；最终成片竖屏直接判流程 FAIL。

## B1–B5 统一更新要求

B1、B2、B3、4090、B4、B5 的新任务必须沿用同一个 `CH-ZX-001`，并保持同一组素材、`card_version`、`rule_version`、`round_id` 和验收口径。B5 不新建卡、不改角色主轴；旧轮次仍保持原状。

## 帧回写要求

Work 端必须从本卡本轮真实输出回写：

- run_A：首帧 / 中帧 / 尾帧
- run_B：首帧 / 中帧 / 尾帧
- 原始视频 task/output ID
- 帧号、时间点、宽高、SHA-256

不能用角色参考图、H3 四宫格、另一轮图片或占位图冒充本卡帧。旧 R35/R37 等历史任务保留原状，不追溯改写。

## 审核结论

审核状态使用：

- `unreviewed`
- `pass`
- `revise`
- `ambiguous`

出现以下情况直接 FAIL：

- 新卡使用 `card_id: null`
- 新卡不是 CH 系列
- 混入第二十元或强制生克补主轴
- 缺少 run_A/run_B
- 缺少 audio_design
- 时长不是 10–11 秒
- 最终输出不是 16:9
- 将 9:16 角色参考图误判为成片画幅
- 用旧动态链规则审核 CH-ZX-001
