---
type: ten-yuan-fire-axis-review-registry
authority_level: L4
knowledge_status: evidence-locked
status: working-registry
axis: fire
pair: zn-x
governance: L1-L6文件权力与知识成熟度双轴协议_v2.0_20260827
may_override_canonical: false
updated: 2026-08-27
---

# zn ↔ x 火轴待审议清单

> 本文件只做 L4 状态索引，不是正本。`pending-review` 达门后停止自动升格，等待授权审议。

## A｜已锁研究资产

### A1｜zn vs x 最近邻分离

状态：`3 positive works + 2 negative guards / pending-review`

正向控制：

1. 《三国演义》关羽·挂印封金，99/98。
   - `zn`：刘备旧盟/旧义保留最终指导资格。
   - `x`：曹赐金银、侯印、人役等现实资源/权限对象。
   - 机制：`zn` 冲突时促使主体主动让渡部分 `x`。

2. 《水浒传》晁盖·梁山劫掠与江州劫法场，99/98。
   - `zn`：不可因取财、脱险或便利任意伤害不抵抗客商/无关百姓。
   - `x`：山寨人马、行动与战利品处分边界真实成立。
   - 机制：`zn` 限制已经成立的 `x` 怎样使用，而不消灭 `x`。

3. 《西游记》唐僧·三打白骨精 / 草寇事件，99/97。
   - `zn`：对人命不可轻率施杀。
   - `x`：对孙悟空的师徒处分/约束权限有现实生效证据。
   - 机制：`zn` 规定关系处分 `x` 的惩戒、保留与终止条件。

反向护栏：

1. 《西游记》孙悟空·弃弼马温，99/98。
   - 御马监职事与马匹管理 `x` 真实成立并主动退出。
   - 弃职主要由官阶低、被轻视与地位比较触发，缺独立通过未来指导资格门的内部原则。
   - 已锁：**放弃 / 退出已掌握 `x` ≠ 自动 `zn`。**

2. 《红楼梦》贾政·笞宝玉遇贾母真实否决后停止，99/98。
   - 当前责打/训子对象层存在现实处分 `x`。
   - 停止链可由“贾母上位现实否决进入 → 贾政服从 → 处分停止”完整解释。
   - 已锁：**`x` 被限制 / 停止使用 ≠ `zn` 自动成立；先检查限制来自内部原则还是外部命令、惩罚压力、现实否决。**

已建立：

`07-Codex大脑库/待审议问题_zn-x内部原则与现实掌握权分层边界_20260827.md`

状态：`L4 / pending-review / may_override_canonical: false`。

### A2｜纯 x 首轮控制

状态：`1 control / 1 work`

- 《红楼梦》刘姥姥·一进荣国府获赠银钱，99/98。
- 完成交付后，银钱进入实际占有、使用与处分边界；无需回原节点逐次批准。
- `zn` 不锁：家庭用途、现实需求与感恩不足以独立通过不可让渡原则、冲突排序和未来指导资格门。

纪律：**外部来源 ≠ 永久 `nx`；完成真实交付并取得直接处分权后，当前对象可转为 `x`。现实用途 / 家庭需求 ≠ 自动 `zn`。**

### A3｜纯 zn 首轮控制

状态：`1 control / 1 work`

- 《水浒传》鲁智深·野猪林救林冲并坚持护送，99/97。
- 原则对象：见不平应介入救护；既然承担救援，就应把被救者送出持续危险。
- 同一原则对象层 `x` 不锁：保护林冲不等于林冲或其安全进入鲁智深的占有、调用、处分或排除边界。

纪律：**pure `zn` 不需要先有一个可掌握的 `x` 对象；保护 / 责任 / 不抛下某人也不等于 `x`。**

### A4｜zn + x 当前共现

状态：`3 controls / 3 works / pending-review`

1. 《三国演义》关羽·挂印封金关键冲突窗口，99/98。
2. 《水浒传》晁盖·梁山劫掠处分，99/98。
3. 《西游记》唐僧·三打白骨精 / 草寇事件，99/97。

已建立：

`07-Codex大脑库/待审议问题_zn-x当前共现与严格补不可等同边界_20260827.md`

纪律：**co-occurrence ≠ strict complement。** 同一当前窗口里两端分别独立成立后，strict 仍必须另做双向缺口、第三因素与对象层测试。

### A5｜strict zn↔x 中 x 是否为 zn 不可替代现实落点

状态：`0 positive + 4 negative guards / 3 independent works / pending-review`

反向护栏：

1. 《三国演义》关羽·挂印封金，99/97。
   - `zn→x` 成立；拿掉曹赐资源后，旧义仍可通过归刘、护送二夫人、离曹寻找刘备等现实对象/行动落地，`x→zn` 不成立。

2. 《水浒传》晁盖·梁山劫掠处分，99/98。
   - `zn→x` 成立；拿掉山寨人马/劫掠处分 `x` 后，“不任意伤害无关者”仍有现实对象并可约束主体自身行为，`x→zn` 不成立。

3. 《西游记》唐僧·三打白骨精 / 草寇事件，99/97。
   - `zn→x` 成立；拿掉师徒处分/约束 `x` 后，“不轻伤人命”仍可约束自身判断、其他关系与其他生命对象，`x→zn` 不成立。

4. 《三国演义》严颜·巴郡守城 → 城失被擒，99/97。
   - 巴郡城防与军政处分 `x` 压力前真实成立，城失被擒后该 `x` 真实断开。
   - `x` 已实际失去后，拒绝威逼屈降的 `zn` 仍继续排序；张飞撤掉威逼、改以义礼后，严颜才自愿归附。
   - 主要新增用途：首份火轴 lifecycle control；同时提供“真实发生的 x 拿掉后 zn 仍持续”的高价值 strict 反向旁证。
   - 不增加 strict negative-guard cross-work 数，因为与关羽同属《三国演义》。

已建立：

`07-Codex大脑库/待审议问题_zn-x严格补中x是否为zn不可替代现实落点边界_20260827.md`

状态：`L4 / pending-review / may_override_canonical: false / strict 0 positive + 4 negative guards / 3 works`。

当前 strict 纪律：

- **一个 `x` 对 `zn` 有帮助，不等于它在 strict 意义下补 `zn`。**
- **`x` 扩大 / 便利 / 强化 `zn` 的执行范围，不等于 `x` 是 `zn` 不可替代的现实落点。**
- **原则保护、对待或评价谁，不等于谁必须先进入主体的 `x` 掌握边界。**
- **现实执行接口 ≠ 不可替代现实落点。**
- **`x` 已真实失去而 `zn` 仍继续排序，是反驳“该 x 为不可替代现实落点”的高价值生命周期证据。**

### A6｜生命周期首轮控制

状态：`1 control / 1 work / evidence-locked`

- 《三国演义》严颜·巴郡守城 → 城失被擒 → 拒绝威逼屈降 → 条件改变后自愿归附，99/97。
- 生命周期链：

```text
x 成立
→ x 真实失去
→ zn 仍持续
→ 关系条件改变
→ zn 继续规定新的可接受边界
```

纪律：

- **`x` 失去 ≠ `zn` 自动失去。**
- 原则后续允许关系改变，不等于原则消失；应先检查它是否只是在重新规定“什么条件下可以改变”。
- lifecycle 与 strict negative guard 分开计数；同一案例可以承担两种研究用途，但不能重复增加独立作品数。

### A7｜名义身份 / 高称号 vs 现实 x 首轮控制

状态：`1 control / 1 work / evidence-locked`

- 《西游记》孙悟空·齐天大圣空衔 → 权管蟠桃园，99/98。
- 阶段 A：齐天大圣名位、府署、礼遇与外部承认均真实存在，但被测“天庭公共职事 / 现实管理对象”层仍是“有官无禄 / 不与管事”，因此该层 `x = off`。
- 阶段 B：玉帝明确授予“权管蟠桃园”职事后，孙悟空进入园务，土地与各类力士围绕其管理位置响应，被测园务管理 `x = on`。
- `zn` 不由称号自动推出；追求高位、外部认可与品级比较不能替代内部不可轻易让渡原则的独立证据。

纪律：

- **名义身份 / 高称号 / 外部承认 ≠ 现实 `x`。**
- **现实 `x` 必须看到被测对象真正进入主体调用、管理、处分或排除边界。**
- **名义位置不是现实 `x` 的必要条件，也不是充分条件。**
- 同一人物同一体系内，称号长期不变而现实权限从 off→on，是高价值最小差异控制。

只增加本研究槽 `1 control / 1 work`，不增加 `zn vs x`、pure-x、co-occurrence、strict、pressure-display 或 lifecycle 既有统计。

## B｜尚未达到 pending-review 的候选槽

1. 纯 `zn` 第二控制。
2. 纯 `x` 第二控制。
3. strict `zn↔x` positive：当前 **0**；禁止从 co-occurrence 或 `zn→x` 单向补自动升级。
4. 伪 `zn+x` 共现：表面两端同框但实际仅一端独立过门。
5. 伪 `zn`：口号/称号/阵营/一次牺牲但无未来指导资格。
6. 伪 `x`：名义持有/代理授权/临时接触但无实际处分边界。
7. 名义身份 / 高称号 vs 现实 `x` 第二控制：优先《三国演义》《水浒传》《红楼梦》的同人相邻最小差异。
8. 被承认 vs 真正成立。
9. 局部本体 vs 整体本体。
10. 压力显影与解压复位。
11. 被替代/改写后的端点保留。
12. lifecycle 第二控制：优先反向结构 `zn` 暂停/退出但 `x` 继续，或压力解除后 `zn` 复位。

## C｜火轴反例优先协议

1. **放弃 / 失去 `x` ≠ 自动 `zn`。**
2. **已成立 `x` 被限制 ≠ 自动 `zn`。** 先查限制来自内部原则、外部命令/现实否决、惩罚恐惧、收益策略还是临时战术。
3. **外部封号 / 认可 / 身份变化 ≠ `zn` 被改写。**
4. **持有对象 ≠ `x` 稳定成立。** 必须查实际使用、调配、处分、否决、排除以及撤权后是否仍成立。
5. **资源来自别人 ≠ 当前一定 `nx`。** 完成交付且原节点不再保留逐次许可/撤回时，优先检查当前 `x`。
6. **保护 / 不抛下某人 ≠ `x`。** 关系责任不能替代实际掌握证据。
7. **`zn` 限制 `x` 的使用方式，不等于 `x` 不成立。** 最短：有权不用 ≠ 没有权。
8. **`zn+x` 当前共现 ≠ strict `zn↔x`。**
9. **`x` 对 `zn` 有现实帮助 ≠ strict `x→zn`。**
10. **原则保护 / 对待 / 评价的对象 ≠ 该对象必须进入 `x`。**
11. **现实执行接口 ≠ 不可替代现实落点。**
12. **真实失去 `x` 后 `zn` 仍持续，是比纯反事实拿掉更强的生命周期证据。**
13. **关系后来改变 ≠ 原 `zn` 必然消失。** 应检验原则是否只是重新界定可接受改变的条件。
14. **名义身份 / 高称号 / 外部承认 ≠ 现实 `x`。** 必须另证被测对象层的实际调用、管理、处分或排除边界。
15. **称号长期存在而现实权限 off→on，说明名义位置与现实掌握必须分层。**

## D｜跨作品晋级门

只有同时满足以下条件才增加 cross-work：

- 使用同一 `criterion_version`；
- 事实与分类均达到锁定门；
- 对象层一致；
- 最近邻已排除；
- 拿掉与反向测试通过；
- 第三因素没有替代端点；
- 不是同作品不同桥段冒充独立作品。

positive、negative guard、lifecycle、pressure display 分账。达到门槛后新建 `pending-review`，仍 `may_override_canonical: false`。

## E｜当前统计

```yaml
zn_vs_x_positive_controls: 3
zn_vs_x_positive_works: 3
zn_vs_x_negative_guards: 2
zn_vs_x_negative_guard_works: 2
pure_zn_controls: 1
pure_zn_works: 1
pure_x_controls: 1
pure_x_works: 1
zn_x_cooccurrence_controls: 3
zn_x_cooccurrence_works: 3
strict_zn_x_complement_controls: 0
strict_zn_x_complement_negative_guards: 4
strict_zn_x_complement_negative_guard_works: 3
nominal_identity_vs_real_x_controls: 1
nominal_identity_vs_real_x_works: 1
pressure_display_positive_controls: 0
pressure_display_negative_guards: 0
lifecycle_controls: 1
lifecycle_works: 1
pending_review_count: 3
```

说明：strict 当前是 **0 positive / 4 negative guards / 3 works**；严颜增加的是同作品不同 `x` 类型的真实生命周期旁证，不增加 strict cross-work。lifecycle 当前为 **1 control / 1 work**。名义身份/高称号 vs 现实 `x` 当前为 **1 control / 1 work**，尚未达到 pending-review。

## F｜下一小时最值得整理

第一优先：寻找第一份真正通过 `x→zn` 不可替代现实落点门的 strict 正例候选。

第二优先：寻找第二份 lifecycle control，最好测试反方向：`zn` 暂停/退出但 `x` 继续，或压力解除后 `zn` 复位。

第三优先：找第一份“表面 `zn+x`、实际仅一端独立过门”的伪共现反例。

第四优先：找第二份“名义身份 / 高称号 / 外部承认 ≠ 现实 `x`”跨作品最小控制。

第五优先：找第二部独立作品的“已成立 `x` 受外部命令/现实否决/惩罚压力限制，但 `zn` 不成立”高纯反例。

第六优先：第二份不同机制 pure-zn / pure-x。