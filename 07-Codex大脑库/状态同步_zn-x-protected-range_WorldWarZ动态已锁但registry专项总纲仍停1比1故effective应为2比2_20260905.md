---
type: ten-yuan-fire-axis-state-sync
authority_level: L4
knowledge_status: evidence-locked
status: state-correction
axis: fire
pair: zn-x
criterion_version: protected-range-risk-test-v1_20260831
source_evidence_commit: 6480461b6ac71fbf4fa188ca4d8f2697e9f0f96d
write_base_main: 3dceaec902110ed0673ac353f020567763e096a1
may_override_canonical: false
may_update_L2: false
control_increment_this_file: 0
work_increment_this_file: 0
created: 2026-09-05
---

# 状态同步｜zn-x protected-range｜World War Z dynamic 已锁，effective 应为 2/2

## 0｜启动与门禁

本轮以写前最新 `main@3dceaec902110ed0673ac353f020567763e096a1` 为准，按 L0/L1 启动纪律重读 `AGENTS.md`、十元关系必读门禁、L1 十元—五行正本 v1.6、火轴实时待审议清单、研究总纲、protected-range current 与最近 commits。current canonical 高于本文件；木轴 `zx↔nx` 只迁移验证方法，不迁移理论结论。

本轮不采普通正例，不新增案例，不修改 L1/L2 canonical；只处理一处会影响后续采样优先级的真实状态漂移。

## 1｜已锁 evidence

`6480461b6ac71fbf4fa188ca4d8f2697e9f0f96d` 已写入《World War Z》(2013) Jerusalem fortified safe zone 的 protected-range 动态失败镜像，并明确：

```yaml
protected_range_dynamic_transition_increment: true
protected_range_dynamic_transition_work_increment: true
protected_range_positive_increment: false
protected_range_negative_increment: false
strict_v2_increment: false
x_scope_increment: false
```

其最小新增边界为：

```text
verified stable protected-range under ingress topology A
+ same boundary/governance x retained
+ same broad zombie physical-ingress risk family

-- ingress topology changes to collective vertical body-pile bypass -->

protected-range can become OFF
```

因此 stable protected-range 必须绑定 `risk_channel + ingress_path + ingress_topology + current_window`；过去在 topology A 的 verified PASS 不得自动外推到 topology B。

## 2｜为什么这是状态修正而不是新 control

写前 current：

- `zn-x火轴待审议清单.md` 仍登记 `protected_range_v1_dynamic_controls: 1 / works: 1`；
- `待审议问题_zn-x-protected-range稳定保护范围经真实risk-test成立边界_20260831.md` 仍登记 `dynamic_transition_controls: 1 / works: 1`；
- `zn-x火轴研究总纲_20260827.md` 仍登记 protected-range `1/1 dynamic`。

但 evidence-layer 已由 The Martian 的 `ON→OFF→ON` + World War Z 的 topology-shift `ON→OFF` 构成两条不同动态机制、两部独立作品。

故 effective current 应为：

```text
protected-range ordinary positive: 4 controls / 4 works   不变
protected-range negative guards:  5 controls / 5 works   不变
protected-range dynamic:           1 / 1 → 2 / 2
```

本文件只登记状态纠偏：`+0 control / +0 independent work`，禁止重复计入 World War Z。

## 3｜zn / x / strict-v2 分账

本轮不新增 `zn`。World War Z 原 evidence 已明确：建墙、接纳、防疫与防御可由制度职责、国家安全与灾难管理解释，不强造内部原则。

`x` 仍锁 institutional actor 的 perimeter governance / controlled admission / defensive access-control；Warmbrunn 个人不扩张成整座 Jerusalem perimeter 的 composite-x。

因此：

```yaml
strict_v2_increment: 0
x_scope_increment: 0
protected_range_dynamic_increment_this_file: 0
```

## 4｜最近邻与高信息增益

- vs The Martian：同 actor / same boundary / same risk channel 下，boundary integrity 破裂再修复，形成 `ON→OFF→ON`。
- World War Z：underlying boundary/governance x 与部署状态仍在，失败由同一 broad risk family 内的 ingress topology 变化触发，形成 `ON→OFF`。

两者不能互相吞并。新增的不是“又一个墙被突破”，而是 protected-range 动态判据从 `boundary state / broad risk label` 提升为 `topology-indexed + window-indexed`。

## 5｜下一高价值缺口

protected-range 普通槽已 pending-review，不再采同机制换皮。下一条真正值得收的是：

> same actor + same boundary + same risk topology 已 verified ON；发生 nominal/technical repair，修复看似完成，但随后同 topology 的真实 risk-test 仍 FAIL。

该镜像可进一步分开：`boundary exists`、`boundary enabled`、`repair completed`、`stable protection restored` 四层。

P0 strict-v2 第二份跨机制 verified positive 与 P1 真正 path-set exhaustion 仍保持更高总优先级；没有 ≥95 的新证据就不提交。

## 6｜治理结论

- effective protected-range dynamic：`2 controls / 2 independent works`；
- 本文件不新增 control/work；
- 不修改 L1/L2 canonical；
- realtime registry、protected-range 专项与研究总纲仍存在 `1/1 → 2/2` 的同步债，后续应在安全全文更新窗口直接同步，而不是再次重复制造 evidence control。
