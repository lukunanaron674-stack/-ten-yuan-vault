---
type: ten-yuan-fire-axis-pending-review
authority_level: L4
knowledge_status: pending-review
status: pending-review
axis: fire
pair: zn-x
question: x是否必须按对象、权限类型、范围、期限、原节点与最终裁定层分账而不得由窄scope倒灌宽scope
criterion_version:
  l1_axis: L1_十元即阴阳五行相反轴正本_v1.6
  x_scope_gate: current-x-scope-distinction-v1_20260830
positive_controls: 4
positive_cross_work_count: 3
boundary_guards: 26
boundary_guard_works: 21
dynamic_transition_controls: 29
dynamic_transition_works: 25
decision_structure_calibration_controls: 6
may_override_canonical: false
created: 2026-08-30
updated: 2026-09-05
---

# 待审议｜zn-x x scope：权限类型、范围、期限与最终归属必须分层

## 0｜候选命题

同一 `x` 不能只用一个粗糙的 `true/false` 描述。

当前 3 部独立作品、4 个高置信正向控制共同支持：

> **`x` 必须至少写清：被测对象、权限类型、权限范围、期限/返还义务、原节点是否保留覆盖/撤回权、最终裁定层。某一窄 scope 的现实 `x=true`，不能自动倒灌为邻近更宽 scope 的 `x=true`。**

current evidence 已扩展到 26 条 / 21 部独立作品的高置信边界护栏，并新增 actor-indexed path-set 与 object/actuator-layer 两道前置门：

> **更宽 scope / 永久最终归属未成立，也不能反向把已经直接生效的窄 current `x` 抹成 `x=false`。formal/source authority、单次现实 compliance、接口持有、作品原生 ownership 标签、稳定 possession/use、成功启动过程、系统级替代路径或跨 actuator/object layer workaround，都不能跨 permission/object layer 自动升级成 stable current control / full disposition / verified target disposition。**

另有 29 条、25 部独立作品的高置信动态迁移控制支持：

> **`x` 的生命周期不只有 off→on / on→off。它可以发生 scope expansion / contraction、permission-type expansion/contraction、quantitative-cap expansion/contraction、single gate ↔ parallel independent gates、joint-threshold ↔ delegated/unilateral execution、external override insertion、内生竞争执行节点插入/退出、time-lock revocability contraction/unlock、credential concentration/redistribution、现实 capture、voluntary release、external dispossession/reacquisition、mandatory procedural unlock，以及 eligible-subset disposition 的开启/缩窄。迁移后不同 permission layer、decision topology 与 execution topology 可以不同步变化。**

核心结构门继续保持：

- future whole-block revocation ≠ same-layer pre-effect veto；
- source decision structure ≠ consultation structure ≠ final decision structure ≠ current execution structure；
- shared permission ≠ joint-threshold；
- hierarchical superiority ≠ permission-type universality；
- formal/source authority ≠ current realized x；
- one-shot successful invocation ≠ stable current x；
- control-interface possession ≠ verified target disposition；
- source-native ownership/title/status label ≠ 十元 x permission bundle；
- permission existence ≠ permission persistence across time；
- task-source / direction-source delta ≠ x-boundary delta automatically；
- nominal conferral / title / authorization ≠ realized current x automatically；
- voluntary release permission ≠ released object remains inside current x boundary；
- current physical possession/use ≠ ultimate title / residual claim；
- permission retained ≠ direct execution path retained；
- edge-veto retained ≠ downstream disposition already present；
- enumerated-interface exhaustion ≠ complete relevant path-set exhaustion；
- system-level target-effect path set ≠ actor-indexed x execution path set；
- same actor + same higher-level target effect ≠ same execution-object / actuator-layer path set。

本文件只到 L4 `pending-review`，不得覆盖 current canonical。

## 1｜正向控制｜4 controls / 3 works

### A｜《西游记》孙悟空龙宫试兵器→金箍棒｜99/98
`trial-use / temporary handling x=true` 不等于 `stable possession/use/disposition x=true`；**能用 ≠ 归我**。

### B｜《三国演义》刘备借荆州｜99/98
current territorial governance/control 与 ultimate-title/permanent ownership 分层；**当前能管 ≠ 永久归我；未来有返还义务 ≠ 当前从未有真实 x。**

### C｜《三国演义》孙策质传国玉玺｜99/98
object possession/transfer 与 represented authority/sovereignty 分层；**能处分象征物 ≠ 能处分其代表权能。**

### D｜《红楼梦》探春受托理家→全园抄检｜99/97
local current management 与 global/final override 分层；**局部真实 x ≠ 全局最终 x。**

## 2｜反向边界护栏｜26 controls / 21 works

早期 16 条继续有效：王熙凤、天蓬元帅、晁盖共同财物、宋江共同授予、袁绍会盟、孙权赤壁公议、《十二怒汉》、红楼第55回临时协理、《小鬼当家》local protected boundary、《Crimson Tide》特殊对象层 mandatory concurrence、Eddard Stark formal-authority vs realized-x、Earth King one-shot compliance vs stable-x、《The Dark Knight》interface vs target-disposition、Frodo possession/use vs destruction-disposition、Mickey start vs stop/terminate、Light source-native owner label vs permission bundle。

后续 evidence-layer 已把 ordinary/boundary control 扩展至 `26 controls / 21 works`。其中 current P1 最关键的两条 path-set 前置护栏如下。

### 2.1｜Deepwater Horizon / Macondo｜actor-indexed path-set guard
source evidence：`f34f226e7a31e80bd4d59de1187cf63d906eac90`。

锁：`system-level target-effect path set ≠ actor-indexed x execution path set`。人工 EDS、自动 deadman/autoshear 与后续外部 ROV 虽都指向封井效果，但不能因 target effect 相同就倒灌成同一主体的 `x`。真正 path-set exhaustion 必须先按 actor 建索引，再审 direct repair、bypass、delegated route、parallel authority、emergency interface、alternate execution node。该证据为 historical control，只增加 boundary control，不增加 independent work。

### 2.2｜United Airlines Flight 232｜object/actuator-layer path-set guard
source evidence：`2f07be9a238fb0969221e5db33c66dcb9de40957`。

锁：`same actor + same higher-level target effect ≠ same x execution-object path set`。三套液压系统全部丧失后，正常液压飞控面执行链已归零；同一机组仍可通过发动机差动推力有限影响整体航迹。若被测 `x` 锁在正常液压飞控面操纵，差动推力属于跨 actuator/object layer substitute，不是原 `x` surviving path；若对象提升为整体航迹控制，差动推力必须进入 relevant path-set。

因此 path exhaustion 除 actor index 外，还必须锁 object layer / actuator layer / target-effect layer。

## 3｜decision-structure 判据校准｜6 controls｜不计 ordinary works

当前校准总数为 6。统一锁：

- individual same-layer veto 是 genuine joint 的充分证据之一，但不是必要条件；
- mandatory multi-node approval threshold 且任一单节点不能独立让同一 final decision 生效，可判 `joint-threshold`；
- `absence of individual veto ≠ unilateral`；
- `joint-unanimous ≠ joint-threshold ≠ shared ≠ unilateral`；
- consultation topology、final-decision topology、execution topology 必须分账；
- source authorization 不自动决定 downstream execution topology。

## 4｜动态迁移控制｜29 controls / 25 works

早期有效控制包括：宋江 expansion、王熙凤 contraction、唐僧 expansion、刘备 expansion、Palpatine delegated scope、Dumbledore override insertion、Grimmauld Fidelius parallel disclosure gates、Otto Octavius competing execution node、Ellen Ripley time-lock revocability contraction、Eduardo Saverin quantitative contraction、Marko Ramius credential concentration、Pokémon capture/release、John Wick dispossession/reacquisition。

**宋江招安后 contraction 已 superseded，不计 current dynamic。** task-source / direction-source 上移本身不足以证明同对象层 permission/node/scope 真实收窄。

后续 evidence-layer 已将 dynamic 更新到 `29 controls / 25 works`。新增机制族至少包括：

- permission/scope expansion 与 contraction 分账；
- reversible quantitative-cap expansion/contraction；
- edge-veto retained while downstream disposition changes；
- conditional/eligible-subset disposition unlock；
- mandatory procedural unlock；
- shared/joint execution ↔ unilateral execution 的拓扑变化；
- direct execution path 丢失但 permission 仍保留；
- execution node / veto / final-decision node 的不同步迁移。

普通 expansion/contraction 正例继续停止；新增只保留 genuine permission/node/scope transition、新反例、判据冲突与状态修正。

## 5｜研究层 current x 最小字段

```yaml
actor: 掌握主体
object: 被测对象
permission_type: 接触/使用/保管/调用/启动/停止/管理/处分/销毁/否决/排除/转让/披露/释放/放弃/授权接口
scope: 局部/全局/对象子集
quantitative_cap: 对可分割资产记录比例/份额/有效上限
term: 临时/期限内/持续
revocability: 是否可撤回
return_obligation: 是否有返还义务
same_layer_pre_effect_veto: 生效前是否存在同层阻断
global_override: 是否存在跨层全局覆盖
ultimate_title: 最终归属是否成立
decision_structure: source decision structure
consultation_structure: single-node / multi-node / broad
final_decision_structure: unilateral / joint-unanimous / joint-threshold / shared / vetoed / contested
execution_structure: unilateral / joint / shared / parallel-independent / autonomous-after-start / contested / vetoed / fragmented
co_decision_nodes: 共同决定节点
source_node: 权限来自何节点
source_native_status_label: 作品内部 owner/title/status 等标签，仅作待映射事实
permission_expiry_or_irreversibility_threshold: deadline/time-lock/不可逆阈值
override_node: 谁仍可现实覆盖
current_same_layer_effect: 当前同层决定能否直接生效
mandatory_multi_node_threshold: 是否必须跨越固定多节点批准门
credential_distribution: mandatory credentials 分散还是集中
represented_authority: 凭证代表权能是否另证
unilateral_effect: 主体单方决定能否直接改变结果
independent_execution_nodes: 可独立生效/绕开/扣留/拒绝节点
endogenous_competing_execution_node: 对象内部独立执行/反向影响节点
competing_anchor: 同窗持续有效竞争掌握节点
object_specific_constraint: 对象本身是否限制 permission 现实效果
realized_effect_test: 被测 permission 是否在 current object layer 现实调用
causal_mapping_verified: 接口到目标结果映射是否验证
path_actor_index: relevant path 属于谁
path_object_layer: 被测路径对象层
path_actuator_layer: 执行/actuator 层
path_target_effect_layer: 目标效果层
relevant_path_set: 同 actor + 同 object/actuator/effect family 的完整路径集合
surviving_relevant_path_count: 当前仍有效路径数
scope_transition: from -> to
permission_type_transition: from -> to
transition_direction: expansion / contraction / redistribution / relinquishment / dispossession / reacquisition
transition_trigger: 真实权限迁移节点
retained_layers: 迁移后仍保留权限层
lost_or_externalized_layers: 迁移后上移/外置/失去权限层
```

以上仅为 L4 方法字段建议，不自动修改 L2 数据结构。

## 6｜统一测试协议

1. 窄 scope 拿掉：拿掉后相关当前行为不再可能直接生效，说明该窄 scope 有现实作用。
2. 宽 scope 反向门：升级更宽 `x` 时另证原节点是否退出、上位是否仍可同层覆盖、期限/返还条件是否跨越、局部决定是否扩展到最终裁定。
3. future revocation 不是 current x 反证；重点查 same-layer pre-effect veto。
4. shared/joint → unilateral 必须证明同层节点不保留 mandatory co-approval / blocking threshold。
5. 共同授权或广泛咨询不得预设最终裁决和执行结构。
6. 多个 co-holder 若任一可独立生效，不得写成 joint-threshold。
7. 广域 senior-command 不得跨 permission type 倒灌特殊对象层 unilateral final-disposition。
8. 正式授权/签署文件只证明 source layer；必须继续观察 current permission 调用时现实节点是否响应。
9. 一次成功 compliance 只锁该次 local act；同窗 competing anchor 持续控制相同 execution node 时，不得升级 stable command x。
10. 持有可操作接口只锁 contact/use opportunity；目标处分权需另证 causal mapping，且 local mapping 与 global override 分账。
11. possession/use 与 destruction-disposition 分账。
12. invoke/start 不自动推出 stop/terminate/revoke/veto。
13. 作品原生 owner/title/status 词不得直接替代十元 x 判定；先逐项验证 current permission bundle。
14. scope / permission expansion 不得把迁移后宽 `x` 倒填到迁移前。
15. scope contraction 后继续逐层检查低层 current `x` 是否保留。
16. 对象内部新增 autonomous/competing execution node 时，把 retained use 与 lost exclusivity/final control 分开。
17. revocability 必须记录 term 与 deadline。
18. 可分割资产必须记录 quantitative cap/share/subset。
19. credential gate 迁移必须区分“持有全部授权凭证”与“downstream effect 已现实完成”。
20. capture/acquisition 必须由 same-object realized effect-test 证明；release/relinquishment 把释放权限与释放后 current x 分开。
21. external dispossession 按 permission type 分账；current possession/use/custody OFF 不自动令 title/claim OFF。
22. task-source / direction-source 变化不自动构成 x-scope transition。
23. edge-veto、downstream disposition、eligible subset 必须分账。
24. permission retained 不等于 direct execution path retained。
25. path exhaustion 前必须先做 actor-indexed relevant path-set completeness audit。
26. direct repair、bypass、delegated route、parallel authority、emergency interface、alternate execution node 必须逐项冻结。
27. system-level alternate node 不得倒灌成主体 x，也不得为了制造 path=0 被静默删掉。
28. 同 actor 仍必须锁 object / actuator / target-effect layer；跨层 workaround 与原 x 分账。
29. 只有同一 actor、同一 object/actuator layer、同一 current window 的 complete relevant path-set 已审计后，才允许写 `surviving_relevant_path_count=0`。
30. 真正 path-set exhaustion 还必须通过该被测层 target-effect reality-test OFF；只枚举接口失败不够。
31. current dynamic `29 controls / 25 works` 已 pending-review，停止普通扩张/收缩与同型 execution-topology 堆量。

禁止倒灌：

```text
能试用 → 所有权
当前治理 → 永久产权
拿着印信 → 代表权力
局部管理 → 全局最终权
稳定持有/使用 → 完整不可逆销毁处分
成功启动 → 能停止/终止/撤销
作品称 owner → 所有十元 x 权限自动为真
作品称 non-owner → 所有十元 x 权限自动为假
共享治理 → 个人单方最终处分
共同授权 → 后续必须逐项共同执行
广泛咨询 → shared/joint final decision
多人持有同类权限 → mandatory joint approval
最高层级/广域 command → 特殊对象层 unilateral final-disposition
正式授权/签署文件 → current realized control
一次命令执行成功 → stable organizational command x
拿到控制接口 + 敌对方声称映射 → verified target-disposition x
迁移后宽 x → 迁移前已经拥有同样宽 x
高层 scope 收窄 → 所有低层 x 都自动消失
permission 当前存在 → 跨越期限后仍永久存在
ownership=true → 忽略可分割份额变化
持有全部 mandatory credentials → downstream effect 已现实完成
任务来源/组织独立性变化 → permission boundary 自动变化
曾拥有 release 权 → release 后对象仍在 current x
被偷/被夺走 current possession → ultimate title/claim 自动归零
仍有 title/claim → current physical custody/use 仍自动为 true
系统仍有目标实现路径 → 被测主体 x 路径仍存在
同一主体仍能影响更高层目标 → 原 object/actuator-layer x 路径仍存在
列出的几个接口都失败 → relevant path-set 已耗尽
```

## 7｜成熟度

```yaml
authority_level: L4
knowledge_status: pending-review
criterion_version: current-x-scope-distinction-v1_20260830
positive_controls: 4
positive_cross_work_count: 3
boundary_guards: 26
boundary_guard_works: 21
dynamic_transition_controls: 29
dynamic_transition_works: 25
decision_structure_calibration_controls: 6
positive_works: [西游记, 三国演义, 红楼梦]
may_override_canonical: false
```

达到 pending-review 后，停止继续堆普通正向、同型普通护栏、普通 expansion/contraction 与同型 execution-topology；后续只收新边界机制、真实动态结构迁移、判据冲突、状态修正。

## 8｜下一步高信息增益

1. **P1 path-set exhaustion**：只收同一 actor、同一 object/actuator layer、同一 current window 下的完整路径耗尽；先做 completeness audit，再验证 surviving relevant paths `n>1 → 1 → 0` 与该层 target-effect OFF。
2. **edge-veto / downstream disposition 分离与撤回**：优先 edge-veto 保留而 downstream disposition 撤回，或 eligible subset 真实缩窄。
3. **新型 execution topology**：只收 trigger/topology 与 Pacific Rim、procedural unlock 等既有机制不同者。
4. **strict-v2**：本专项不替代 strict gate；第二份 strict positive 或 deferred 新证据回到 strict 专项处理。
5. deferred 仅在新证据出现时复审。
6. 等授权审议，不自动向 L2 升格。

## 9｜不修改 canonical

本文件不修改 L1、`x/zn` 信息卡与准度卡、`zn补x_补卡` 或 strict v2 gate。

TASK_DONE:ZNX_XSCOPE_PENDING_REVIEW_4POS_3WORKS_26GUARDS_21WORKS_29DYNAMIC_25WORKS_6CALIBRATION_20260905