---
type: f12-run-record
version: v0.7
status: edge-loaded
created: 2026-05-17
---

# v0.7｜Edge 运行验收记录

## 0. 当前结果

2026-05-17 用户在 Microsoft Edge 中成功加载 F12 Controller v0.7 扩展。

已确认画面：

```text
Edge 扩展页已加载 F12 Controller v0.7
Side Panel 已出现：F12 Controller v0.7 / F12 总控塔 v0.7
当前工蜂区域可显示 ChatGPT 标签页下拉框
运行状态显示：manual_pause / stopped
任务列表、载入任务、上一轮、下一轮、重置断点等按钮已出现
```

结论：

```text
Edge 可作为当前 v0.7 的主测试浏览器。
Chrome 开发者模式不稳定时，优先使用 Edge 跑 unpacked extension。
```

## 1. 截图中的关键状态

截图中 Selector 健康检测显示：

```text
输入框 Missing
发送按钮 Missing
停止按钮 Missing
最后回复节点 Missing
```

这不一定是插件失败。更可能原因是：

```text
当前 activeTab 尚未绑定真正的 ChatGPT 对话页；
或当前选择的是扩展页 / 非聊天页 / 未打开对话输入框的页面。
```

下一步必须：

```text
1. 打开一个真正的 chatgpt.com 对话页。
2. 在 Side Panel 的 ChatGPT 标签页下拉框里选中该对话页。
3. 点击“选择当前标签页为 activeTab”。
4. 再刷新健康检测。
5. 预期输入框 / 发送按钮 / 最后回复节点至少部分变为 OK。
```

## 2. 当前 v0.7 验收状态

| 项目 | 状态 | 备注 |
|---|---|---|
| Edge 加载 unpacked extension | 通过 | 用户截图确认 |
| Side Panel 总控塔显示 | 通过 | 右侧面板已出现 |
| content.js 默认不乱发 | 待继续观察 | 当前运行状态 manual_pause / stopped，符合预期 |
| activeTab 绑定 | 待测试 | 当前截图显示 activeTab 未选择 |
| Selector 健康检测 | 待测试 | 当前 Missing，需绑定真实 ChatGPT 对话页后再测 |
| 任务导入 | UI 已出现 | 需实测 ---TASK--- |
| 发送当前 | 待测试 | 必须在 activeTab OK 后测 |
| 自动续跑 | 待测试 | 先测单轮，再测自动 |
| R5 CarryPacket | 待测试 | 跑到 R5 或手动触发 |

## 3. 下一步测试流程

```text
T1. 打开 chatgpt.com 新对话页。
T2. 在 Side Panel 选择这个标签页为 activeTab。
T3. 看 Selector 健康检测是否变为 OK。
T4. 粘贴一个最小测试任务：
    请回复 TASK_DONE_TEST
T5. 点击“发送当前”。
T6. 确认只在 activeTab 发出，不影响其他页面。
T7. 测试自动续跑 2 轮。
T8. 测试 R5 CarryPacket 生成。
T9. 刷新页面后确认断点不丢。
```

## 4. 若健康检测仍 Missing

让 Codex 检查：

```text
adapter-chatgpt.js 的 selector 是否适配当前 ChatGPT DOM：
- #prompt-textarea
- div[contenteditable="true"][role="textbox"]
- textarea
- [data-testid="send-button"]
- [data-testid="composer-submit-button"]
- button[type="submit"]
- [data-message-author-role="assistant"]
```

并加入“检测当前页面 URL 是否为 chatgpt.com 对话页”的提示，避免在扩展页或设置页误报 Missing。

## 5. 一句话

v0.7 已经成功上船，下一步不是重造，而是绑定真正 ChatGPT activeTab，完成 selector 健康检测与单轮发送试航。
