---
type: f12-run-record
version: v0.9
status: engineering-stable
created: 2026-05-17
marker: F12_V09_ENGINEERING_STABLE
---

# v0.9｜工程稳定版完成记录

## 0. 完成标记

```text
F12_V09_ENGINEERING_STABLE
```

结论：v0.9 已完成工程稳定化阶段。当前版本已从“能运行的扩展原型”升级为“可模块化、可测试、可导出、可回滚、可半自动归档”的工程版。

## 1. 修改 / 新增文件总览

本轮修改 / 新增共 19 个文件。

### 新建 core 模块 6 个

| 模块 | 职责 |
|---|---|
| `core/state-schema.js` | schema 版本管理、validate / normalize / migrate |
| `core/lease-lock.js` | 租约锁 acquire / release / isValid |
| `core/message-router.js` | sendToActiveTab / pingWorker / executeTaskOnWorker |
| `core/background-controller.js` | start / stop / pause / resume / retry / forceComplete / runNRounds 等 handler |
| `core/error-snapshot.js` | 错误快照 + Debug Report Markdown 生成 |
| `core/migrations.js` | v0.7→v0.8→v0.9 迁移链 |

### 修改文件 7 个

| 文件 | 变化 |
|---|---|
| `manifest.json` | 加入 `type: module`，版本升至 v0.9.0 |
| `background.js` | 从内联大文件拆分为 import 调度逻辑 |
| `sidepanel.html` | 增加 Debug Report、批量工单、HEAD manifest、handoff 下载、测试包按钮 |
| `sidepanel.js` | 增加新 import 与新 handler |
| `sidepanel.css` | 增加 v0.9 按钮样式 |
| `core/github-handoff.js` | 增加批量写入、BuildManifestPatch、BuildCodexArchiveInstruction |
| `core/obsidian-exporter.js` | 增加 unique URI、safeEncode、vault 路径预览 |

### 新建 Playwright 测试目录

```text
playwright-runner-v0.9/
├── package.json
├── README.md
├── fixtures/chatgpt-mock.html
├── fixtures/tasks.example.md
├── scripts/run-headed.js
└── tests/extension-smoke.spec.js
```

### 新建文档

```text
MIGRATION.md
```

用于记录 v0.7→v0.8→v0.9 迁移与回滚指南。

## 2. 验收标准对照

| # | 标准 | 状态 |
|---|---|---|
| 1 | 功能不倒退 | 通过，content.js 仍 dormant |
| 2 | background.js module 加载 | 通过，`type: module` |
| 3 | state 导入前校验备份 | 通过，validate + BACKUP_* 键 |
| 4 | schemaVersion 显示 | 通过，Side Panel 状态区 |
| 5 | manual_pause 可恢复 | 通过，暂停 / 继续按钮 |
| 6 | CarryPacket 复制下载 | 通过 |
| 7 | GitHub 单个 + 批量工单 | 通过 |
| 8 | Obsidian URI + .md | 通过 |
| 9 | Debug Report | 通过，复制 / 下载 |
| 10 | Playwright fixture | 通过 |
| 11 | README 完整 | 通过 |
| 12 | content.js dormant | 通过 |
| 13 | 只有 activeTab 执行 | 通过 |
| 14 | 多页面不并发 | 通过，leaseLock |

## 3. 当前阶段判断

v0.9 的价值不是新增大量功能，而是完成了工程骨架稳定化：

```text
模块化调度
状态 schema
租约锁
消息路由
错误快照
迁移回滚
Playwright fixture
Obsidian / GitHub handoff
```

这意味着 F12 总控已经具备继续进入 v1.0 的基础条件。

## 4. 下一步建议

下一步不建议继续盲目加功能。优先进行试航：

```text
T1. Edge 刷新扩展。
T2. 打开真实 ChatGPT 对话页。
T3. 选择 activeTab。
T4. 用最小任务 TASK_DONE_TEST 测试单轮。
T5. 试跑 2 轮。
T6. 生成 R5 CarryPacket。
T7. 导出 state.json。
T8. 导出 Debug Report。
T9. 运行 Playwright fixture smoke test。
```

## 5. v1.0 候选方向

v1.0 可以围绕“生产稳定性”展开，而不是继续堆 UI：

```text
1. 真实页面 selector 自愈与 profile 机制
2. 任务模板库项目化
3. CarryPacket manifest 总索引
4. Codex / GitHub 写入工单一键生成
5. Playwright headed 真实页试航
6. Tampermonkey 轻量备用版
7. 多项目工作区
8. v1.0 最终使用手册
```

## 6. 一句话

v0.9 是把 F12 总控从“能开的小船”升级成“能修、能测、能回滚、能交班的工程舰”。下一步该试航，不该立刻拆船重造。
