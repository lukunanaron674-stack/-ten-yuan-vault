---
type: f12-progress
area: F12总控载体
version: v1.2-draft
status: active
created: 2026-06-03
---

# F12 v1.2 页面控制器与进度保存

## 1. 当前保存点

本轮把 F12 的方向从“单个边栏发任务”继续收束到：

```text
总控管理任务库和页面池
页面控制器负责自己的输入框、发送、完成锁定和断点
本地桥让 Codex 可以查看状态、召回页面控制器、低耗操作
Obsidian 负责保存 F12 的版本记忆和进度快照
```

当前运行代码位置：

```text
C:\Users\19308\Documents\Obsidian\ten-yuan-vault\04-F12总控载体\extension-v0.7
```

当前桥地址：

```text
http://127.0.0.1:17312
```

当前操作入口：

```powershell
node C:\Users\19308\.codex\skills\ten-yuan-f12-operator\scripts\f12.js status
node C:\Users\19308\.codex\skills\ten-yuan-f12-operator\scripts\f12.js multi-panel <tabId>
```

## 2. 已确认能力

| 能力 | 当前状态 | 证据 |
|---|---|---|
| Edge 扩展本地桥 | 可用 | `status` 返回 bridge ok，端口 17312 |
| LazyManChat 支持 | 已加入 | manifest 已包含 `https://lazymanchat.com/*` |
| 页内 Page Kernel | 可注入 | `content.js` 使用 `TY_F12_PAGE_KERNEL_V12` |
| 边栏召回页内控制器 | 已接入 | 页面池按钮“打开页内控制器”发送 `SHOW_PANEL` |
| SHOW_PANEL 桥命令 | 已验收 | 最近返回 `status: panel_opened` |
| ChatGPT role-lock 测试 | 已有通过样本 | tab `1438824682` 已完成 `TASK_DONE:R1/1` 并归档 |
| 多页面页面池 | 已可见 | `multiTabs.tabs` 能记录 ChatGPT / LazyMan 页面 |
| 任务类别库 | 已实装 | 总控新增银矿 / 动态链 / 五大主题 / 视觉 / 故事 / 反例 / 归档 |
| 页面进度字段 | 已实装 | 页面内核保存 category / project / currentRound / status / lastError |
| 进度快照导出 | 已实装 | 总控新增“导出进度快照”按钮 |
| v1.2 静态验收 | 已实装 | `npm run verify:v12` |

## 3. 当前运行状态快照

保存时间：2026-06-03

| 字段 | 当前值 |
|---|---|
| schemaVersion | `1.1.0` |
| activeTabId | `1438824661` |
| activeTitle | `随便聊聊 · LazyMan` |
| activeUrl | `https://lazymanchat.com/chat?...` |
| selectedTabId | `1438824661` |
| 总控轮次 | `R1/3` |
| 总控状态 | `manual_pause` |
| LazyMan 旧页 | `1438824661`，paused，R1/1，lastMessage=`SHOW_PANEL` |
| LazyMan 新页 | `1438824699`，online，R0/0，lastMessage=`SHOW_PANEL` |
| 最近桥命令 | `MULTI_COMMAND SHOW_PANEL` |
| 最近结果 | `panel_opened` |

注意：当前 LazyMan 有新旧两个 tab 记录。实际使用时应先点“刷新页面池”，选当前页面，再点“绑定选中页”和“打开页内控制器”。

## 4. 当前代码更改记忆

1. `manifest.json`
   - 支持 ChatGPT / chat.openai.com / LazyManChat。

2. `content.js`
   - 页面内核为 `TY_F12_PAGE_KERNEL_V12`。
   - 页面自己保存任务、轮次、状态。
   - 完成判断走 role-lock：先锁新 assistant 节点，再等 `TASK_DONE:Rn/Total`。
   - 页内控制器可被 `SHOW_PANEL` 重新打开。

3. `sidepanel.html` / `sidepanel.js`
   - 页面池增加“打开页内控制器”。
   - 选中页后发 `SP_MULTI_COMMAND + SHOW_PANEL`。

4. `background.js`
   - 多页命令可分发到指定 tab。
   - `SEND_CURRENT` / `AUTO_RUN` 走 dispatch-and-return，避免桥长时间阻塞。

5. `local-bridge/cli.js`
   - 已有 `multi-panel <tabId>` 召回页内控制器。

6. v1.2 增量
   - `background.js` 新增 `DEFAULT_TASK_LIBRARY`。
   - `background.js` 新增 `SP_SET_CATEGORY` / `SP_LOAD_CATEGORY_TASKS` / `SP_EXPORT_PROGRESS_MD`。
   - `sidepanel.html` 新增“任务库”选择、“载入类别模板”、“导出进度快照”。
   - `sidepanel.js` 页面池显示 category 和 error 提示。
   - `content.js` 页面内核保存 category / project / lastError，并在心跳中回传。
   - `local-bridge/cli.js` 新增 `category` / `category-load` / `progress-md`。
   - `scripts/verify-v12.js` 新增 v1.2 静态验收。
   - 类别模板已移除旧式 `TASK_DONE_R`，改为服从页面内核附加的 `TASK_DONE:Rn/Total` 完成标记。

## 5. 下一步应改的库中 F12

目标不是再堆一个新面板，而是把“任务库”和“进度保存”坐实。本页已完成第一版实装，下一步是重载 Edge 扩展后进行页面级验收。

## 5.1 使用方式

1. Edge 扩展页点“重新加载”。
2. 刷新 ChatGPT / LazyManChat 页面。
3. 打开 F12 边栏，点“刷新页面池”。
4. 在“任务库”选择类别，例如“动态链”或“银矿”。
5. 设置总轮数，点“载入类别模板”。
6. 选中目标页面，点“绑定选中页”。
7. 点“打开页内控制器”或“发送到选中页 / 选中页自动跑”。
8. 需要保存时点“导出进度快照”。

## 5.2 本地桥命令

```powershell
node C:\Users\19308\.codex\skills\ten-yuan-f12-operator\scripts\f12.js category dynamic
node C:\Users\19308\.codex\skills\ten-yuan-f12-operator\scripts\f12.js category-load dynamic 3
node C:\Users\19308\.codex\skills\ten-yuan-f12-operator\scripts\f12.js progress-md C:\Users\19308\Desktop\f12-progress.md
node C:\Users\19308\.codex\skills\ten-yuan-f12-operator\scripts\f12.js multi-status
```

## 5.3 静态验收

```powershell
cd C:\Users\19308\Documents\Obsidian\ten-yuan-vault\04-F12总控载体\extension-v0.7
npm run verify:v12
```

当前结果：`v1.2 verification passed`。

### A. 总控任务库

| 类别 | 用途 |
|---|---|
| 银矿 | 案例采矿、评分、入库 |
| 动态链 | 十元关系、力流、母型 |
| 五大主题 | 时间 / 本体 / 空间 / 因果 / 命运归仓 |
| 视觉 | 图像风格、构图、无 AI 味 |
| 故事 | 一句话、三幕式、角色冲突 |
| 反例 | 伪矿、误判、污染桥 |
| 归档 | CarryPacket、Obsidian / GitHub 回流 |

### B. 页面进度快照

每个页面控制器至少要保存：

```json
{
  "tabId": "页面ID",
  "url": "页面地址",
  "title": "页面标题",
  "category": "任务类别",
  "project": "项目名",
  "tasksTotal": 0,
  "currentRound": 0,
  "status": "online | idle | running | paused | done | failed",
  "lastMessage": "最近状态或摘要",
  "lastError": "最近错误",
  "updatedAt": "更新时间"
}
```

### C. 总控积累方式

总控不应该只显示当前任务文本，还应该积累：

1. 任务类别库。
2. 每个类别的默认模板。
3. 每个页面的进度卡。
4. 每个页面最后一次完成状态。
5. 每个归档轮输出的 CarryPacket 入口。

## 6. 下次施工口令

```text
继续验收 F12 v1.2：任务类别库 + 页面进度快照。

要求：
1. 重载 Edge 扩展并刷新页面。
2. 选择任务类别并载入 3 轮测试模板。
3. 发送到 LazyMan / ChatGPT 选中页。
4. 检查页面池是否显示类别、进度、状态和错误。
5. 导出进度快照，确认 markdown 可写入 Obsidian。
```

## 6.1 2026-06-03 进度保存补丁

本次已把“库中的 F12”再保存一轮，重点不是开跑，而是防止旧模板污染新内核。

已完成：

1. `background.js` 增加 `TASK_LIBRARY_VERSION = v1.2-marker-20260603`。
2. 浏览器旧 `chrome.storage` 里如果保存过旧任务库模板，重载后会自动切回新版 `DEFAULT_TASK_LIBRARY`。
3. `background.js` 默认轮次生成移除旧 `TASK_DONE_R`，统一改为“完成时按页面控制器附加的完成标记输出”。
4. `sidepanel.js` 默认 10 / 12 / 30 / 100 轮生成也移除旧 `TASK_DONE_R`。
5. `core/task-parser.js` 和 `core/task-templates.js` 底层模板库同步移除旧 `TASK_DONE_R` / `TASK_DONE_...`。
6. 页面进度快照新增 `ageSec`，旧页面会按心跳年龄显示 `offline`，避免把五月份旧页面误认为在线。
7. 本地桥 `multi-status` 在线窗口改为 120 秒，减少刚刷新页面池后过快误判 offline。
8. `scripts/verify-v12.js` 新增检查：任务库版本锁、默认生成器、core 模板、CLI 心跳窗口。

静态验收：

```powershell
cd C:\Users\19308\Documents\Obsidian\ten-yuan-vault\04-F12总控载体\extension-v0.7
npm run verify:v12
```

当前结果：

```text
v1.2 verification passed
```

已导出快照：

```text
C:\Users\19308\Documents\Obsidian\ten-yuan-vault\04-F12总控载体\F12进度快照_2026-06-03.md
```

运行态证据：

```text
bridge: http://127.0.0.1:17312
taskLibraryVersion: v1.2-marker-20260603
currentCategory: dynamic
loaded category tasks: dynamic / 3
lastStatus: category_tasks_loaded
```

注意：

本补丁改动了扩展代码。要让 `ageSec` 快照和新版 multi-status 显示在 Edge 扩展运行态生效，需要再次在 Edge 扩展页点“重新加载”，然后刷新 ChatGPT / LazyManChat 页面。

## 6.2 2026-06-03 运行态验收记录

本次已完成低耗运行态验收，没有主动发送真实任务。

已确认：

1. 本地桥在线：`http://127.0.0.1:17312`。
2. 扩展状态可读，`taskLibraryVersion = v1.2-marker-20260603`。
3. 总控类别库可载入，当前为 `dynamic / 动态链`。
4. 总控已载入 3 轮类别任务，`lastStatus = category_tasks_loaded`。
5. 当前 LazyMan 活跃页可被页面池发现：`1438824670`。
6. 对 `1438824670` 执行 `multi-panel` 成功，返回 `panel_opened`。
7. 页面池能记录 `category = dynamic`、`role = executor`、`status = online`、`lastMessage = SHOW_PANEL`。
8. 进度快照可导出到 Obsidian：

```text
C:\Users\19308\Documents\Obsidian\ten-yuan-vault\04-F12总控载体\F12进度快照_2026-06-03_最终验收前.md
```

本次没有做：

1. 没有往 LazyMan / ChatGPT 发送新任务。
2. 没有触发自动续跑。
3. 没有做长轮次测试。

剩余最后验收：

```text
找一个空白或专用测试页，载入 1 轮短任务，发送后确认：
1. 页面输出 TASK_DONE:R1/1。
2. 页面池从 R1/1 进入 done / idle。
3. 最新回复可归档。
4. 快照能记录该页最终状态。
```

心跳窗口调整：

原本 120 秒后页面会显示 offline，人工查看时过紧。本轮已把 `PAGE_HEARTBEAT_ONLINE_MS` 从 `120000` 调整为 `600000`，也就是 10 分钟。这样刚完成召回或刷新页面池后，不会太快误判离线；旧页面仍会因为 `ageSec` 很大而显示 `offline`。

## 7. 封存句

F12 的下一步不是多跑几轮，而是让每一类任务、每一个页面、每一次断点，都能被总控记住并回到 Obsidian。
