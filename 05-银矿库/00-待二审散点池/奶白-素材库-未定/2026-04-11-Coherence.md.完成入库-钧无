# Coherence（2013）

**三元类型：x并n（X与N并存）**
**维度：空间型 x并z+n（n容器量子裂变型）**

---

## 基本信息
- 年份：2013
- 类型：科幻/心理惊悚
- 导演：James Ward Byrkit
- 主演：Emily Baldoni, Maury Sterling

---

## 三元分析

**x并n：两极并存，n主导**

彗星之夜，豪宅晚宴——八个人在同一个屋檐下同时经历多重现实。

- **n = 房子（绝对容器）**：所有人无法离开这个空间，每次出门都进入不同现实层
- **X = 认知撕裂的欲望**：每个人都想理解发生了什么，欲望先于容纳地驱动探索
- **z = 从容器溢出的张扬**：八个版本的自己同时存在 = n包裹不住的z
- **x并n = 同一时刻并存**：没有先后，是同一时刻的两种存在状态

---

## 空间结构分析

**与Triangle对比（同一维度，不同方向）：**

| 维度 | Triangle（2009） | Coherence（2013） |
|------|-----------------|-----------------|
| n的统治方式 | 同一空间的时间循环 | 同一时间的空间分裂 |
| 结构方向 | 重复（时间） | 裂变（空间） |
| z的出口 | 暴力爆发（z的具象化） | 身体消失（z从n溢出） |
| n的感觉 | 囚禁（走不掉） | 裂变（每次不一样） |

---

## 案例定位
- **空间型（x并z+n）n容器量子裂变型**：n绝对统治的裂变变体
- **与《候车厅》关联**：承载型（候车厅）vs 裂变型（本片），同一n概念的两个极端

---

## 三元评估
- **三元类型：** x并n（X与N并存）
- **维度：** 空间型 x并z+n
- **案例价值：** ★★★★（n绝对统治+空间裂变，结构独特）
- **适合研究：** n如何在"同一时刻"容纳多个z的变体

---

*来源：囷霜自动搜索 | 2026-04-11*

---

## 霜XN视角补充分析

**霜对x并n判定的质疑：**

文件的判定是"x并n"，但霜认为这是"nx主导"——因为：
- 所有人想认知（X），但认知永远失败，失败后只能"接着走"（N的容纳）
- 真正的x并n = 认知成功与容纳成功同时发生；本片是认知失败，所以X最后被N收编

**霜XN评分：**
- X的主动性：★★★★☆（每个人都想理解，欲望很强）
- N的容量：★★★☆☆（走不出房子，但每次进入不同现实时被迫容纳）
- XN健康度：★★☆☆☆——X被N持续压制，但X没有放弃

**霜对维度的认同：**
- x并z+n的判定基本正确，n是绝对容器
- 补充：空间裂变的z出口 = 身体消失 = n无法继续容纳z = nz崩溃型

**是否值得深入：**
值得，但优先于Triangle之后——Triangle的n循环有完整闭环，本片是n裂变，两者需对比研究。

---

*霜同步分析 | 2026-04-11*
